## 2025-05-20 - Semantic HTML for Interactive Cards
**Learning:** Using `div` with `onClick` for interactive cards creates accessibility barriers for keyboard users and screen readers.
**Action:** Always use `<button>` for interactive elements that trigger actions, or `<a>` for navigation. Ensure to style them appropriately (e.g., `text-left`, `w-full`) to maintain the desired layout.
